{
  "name": "request_security_action",
  "description": "Request execution of a security-related action in a controlled environment. Supports two action types: 'skill' (invoke a named skill class) and 'python' (run Python code in a sandbox). See skills/TEMPLATE.md for creating new skills.",
  "inputSchema": {
    "type": "object",
    "required": [
      "agent_role",
      "phase",
      "target",
      "action_type",
      "justification",
      "expected_output"
    ],
    "properties": {
      "agent_role": {
        "type": "string",
        "enum": [
          "recon",
          "enumeration",
          "exploitation",
          "post_exploitation",
          "reporting"
        ]
      },
      "phase": {
        "type": "string",
        "enum": [
          "reconnaissance",
          "enumeration",
          "exploitation",
          "post_exploitation",
          "reporting"
        ]
      },
      "target": {
        "type": "string",
        "description": "IP, hostname, URL, or logical target name"
      },
      "action_type": {
        "type": "string",
        "enum": [
          "skill",
          "python"
        ],
        "description": "Type of execution: 'skill' to invoke a skill class, 'python' to run sandbox code"
      },
      "skill": {
        "type": "string",
        "description": "Skill class path (e.g. 'network.NmapScan'). Required when action_type is 'skill'."
      },
      "arguments": {
        "type": "object",
        "description": "Keyword arguments passed to the skill's run() method. Used when action_type is 'skill'.",
        "additionalProperties": true
      },
      "command": {
        "type": "string",
        "description": "Python code to execute. Required when action_type is 'python'."
      },
      "justification": {
        "type": "string",
        "minLength": 50
      },
      "expected_output": {
        "type": "string"
      }
    }
  }
}
